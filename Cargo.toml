[package]
name = "rtiow"
version = "0.1.0"
edition = "2024"

[profile.release]
opt-level = 3
lto = "fat"

[dependencies]
glam = { version = "0.30.9", features=["rand"]}
minifb = "0.28.0"
rand = "0.9.2"
rayon = "1.11.0"

{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Cargo Run (Release)",
            "cargo": {
                "args": [
                    "run",
                    "--release"
                ]
            },
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}